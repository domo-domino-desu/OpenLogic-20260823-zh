% Part: lambda-calculus
% Chapter: introduction
% Section: conversion

\documentclass[../../../include/open-logic-section]{subfiles}

\begin{document}

\olfileid{lam}{int}{con}
\olsection{转换与归约}

λ-演算中有三种转换或归约。我们将会看到，转换与归约的区别在于“变换”是单向的还是双向的。

α-转换涉及变元的重命名。很自然会认为 $\lambda x.x$ 和 $\lambda y.y$ 表示同一个函数（恒等函数），我们将此想法形式化如下：

\end{document}