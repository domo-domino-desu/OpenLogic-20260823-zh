% open-logic-cjk.sty
% 中译本正文含大量中文字符，pdfTeX 默认字体（Latin Modern 等）没有中文字形，
% 必须用 lualatex 编译（Makefile 里 latexmk 的默认 PDF 引擎已经改成
% -pdflatex=lualatex）。直接用 ctex 宏包接入中文排版支持，引擎判断、字体
% 加载都交给 ctex 自己处理，这里不手写 \ifluatex/\ifxetex 分支。
% fontset=fandol 用 TeX Live 自带的 Fandol 字族（FandolSong/FandolHei/…），
% 不依赖系统装了什么字体，本机和 CI 用的是同一份 texmf 树里的字体文件。
%
% TeX Live 2024 的 luatexja-compat 会用 \newcommand 定义
% \printglossary，与 memoir 已有的同名命令冲突。加载 ctex 时先暂存并释放
% memoir 的定义，待 luatexja 完成兼容层初始化后再恢复功能更完整的版本。
\let\olpmemoirprintglossary\printglossary
\let\printglossary\undefined
\RequirePackage[fontset=fandol]{ctex}
\let\printglossary\olpmemoirprintglossary

% 正文译文会直接输入 Unicode 希腊字母（如 λ-演算、β-归约）。Latin
% Modern 的文本字体不含这些字形，LuaLaTeX 因而只报告 Missing character
% 而不会回退到数学字体。New Computer Modern 同时覆盖正文中的希腊字母和
% 本书所需的 Unicode 数学字形；fontsetup 也会让正文与数学字体保持配套。
% Load the legacy AMS command aliases first (the sources use e.g. \restriction),
% then let unicode-math replace their glyph definitions with New CM Math.
\RequirePackage{amssymb}
\RequirePackage[default]{fontsetup}

% 与 HoTT Book 中译本一致：正文的 \textbf 使用黑体强调，而不是给宋体
% 机械加粗。这里只重定义 \textbf；标题样式直接使用 \bfseries，因此仍由
% 原来的 CJK main family 和各标题样式控制。
\setCJKfamilyfont{zhemph}{FandolSong-Regular}[
  Extension = .otf,
  BoldFont = FandolHei-Regular,
  ItalicFont = FandolKai-Regular,
  BoldItalicFont = FandolKai-Regular,
  BoldItalicFeatures = {FakeBold=2.5},
]
\let\olporiginaltextbf\textbf
\renewcommand{\textbf}[1]{%
  \ifmmode
    \olporiginaltextbf{#1}%
  \else
    {\CJKfamily{zhemph}\bfseries #1}%
  \fi}
